import { ssrRenderAttrs, ssrRenderAttr, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/custom-api/settings-menu.png";
const _imports_1 = "/custom-api/models-page.png";
const _imports_2 = "/custom-api/openai-codex-provider.png";
const _imports_3 = "/custom-api/providers-section.png";
const _imports_4 = "/custom-api/open-slot-entry.png";
const _imports_5 = "/custom-api/custom-api-panel-current.png";
const _imports_6 = "/custom-api/provider-slots-current.png";
const _imports_7 = "/custom-api/configure-connection-current.png";
const _imports_8 = "/custom-api/request-url-current.png";
const _imports_9 = "/custom-api/endpoint-preset-current.png";
const _imports_10 = "/custom-api/custom-headers-current.png";
const _imports_11 = "/custom-api/fetch-available-models-current.png";
const _imports_12 = "/custom-api/fetch-results-empty.png";
const _imports_13 = "/custom-api/check-gateway-health-button.png";
const _imports_14 = "/custom-api/check-gateway-health-result.png";
const _imports_15 = "/custom-api/manual-model-add-current.png";
const _imports_16 = "/custom-api/model-management-current.png";
const _imports_17 = "/custom-api/advanced-settings-original.png";
const _imports_18 = "/custom-api/select-custom-model-original.png";
const _imports_19 = "/custom-api/model-options-original.png";
const _imports_20 = "/custom-api/start-conversation-original.png";
const __pageData = JSON.parse('{"title":"Custom API","description":"Learn how to configure your own API provider in Breezell instead of using the official quota.","frontmatter":{"title":"Custom API","description":"Learn how to configure your own API provider in Breezell instead of using the official quota."},"headers":[],"relativePath":"custom-api.md","filePath":"custom-api.md"}');
const _sfc_main = { name: "custom-api.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="custom-api" tabindex="-1">Custom API <a class="header-anchor" href="#custom-api" aria-label="Permalink to &quot;Custom API&quot;">​</a></h1><p>If you do not want to use the official Breezell quota, or if you want to use your own API key, you can configure a custom API provider in the Breezell settings page.</p><p>This allows you to connect Breezell to your own model provider, custom gateway, API key, and model name.</p><h2 id="open-the-breezell-settings-page" tabindex="-1">Open the Breezell settings page <a class="header-anchor" href="#open-the-breezell-settings-page" aria-label="Permalink to &quot;Open the Breezell settings page&quot;">​</a></h2><p>To start, find the settings icon in the top-right area of the editor.</p><p>The settings icon looks like a small gear icon and is located near the other toolbar icons.</p><p>Click the settings icon to open the settings menu.</p><p>At the top of the menu, click <strong>Breezell Settings</strong>.</p><p>After clicking <strong>Breezell Settings</strong>, the Breezell settings page will open.</p><p><img${ssrRenderAttr("src", _imports_0)} alt="Breezell settings menu with Breezell Settings selected"></p><h2 id="go-to-the-models-page" tabindex="-1">Go to the Models page <a class="header-anchor" href="#go-to-the-models-page" aria-label="Permalink to &quot;Go to the Models page&quot;">​</a></h2><p>After opening Breezell settings, you will usually enter the <strong>Models</strong> page by default.</p><p>If you are not on this page, look at the left sidebar and click <strong>Models</strong>.</p><p>The <strong>Models</strong> page is where you can manage available models, official models, third-party providers, and custom API providers.</p><p>On this page, you will see several enabled models at the top, such as Claude, GPT, Gemini, Kimi, and Grok models.</p><p>Below the model list, you will see the <strong>Providers</strong> section.</p><p><img${ssrRenderAttr("src", _imports_1)} alt="Breezell Models page with the Providers section"></p><h2 id="choose-a-provider" tabindex="-1">Choose a provider <a class="header-anchor" href="#choose-a-provider" aria-label="Permalink to &quot;Choose a provider&quot;">​</a></h2><p>Breezell supports many different model providers.</p><p>For example, you can find providers such as:</p><ul><li>OpenRouter</li><li>Anthropic</li><li>OpenAI</li><li>OpenAI Codex</li><li>Kimi Code</li><li>GitHub Copilot</li><li>DeepSeek</li><li>Gemini</li><li>Groq</li><li>Mistral</li><li>Qwen</li><li>Google Vertex AI</li><li>Microsoft Azure</li><li>NVIDIA NIM</li><li>Together AI</li><li>Cohere</li><li>Fireworks</li><li>DeepInfra</li><li>Cerebras</li><li>Perplexity</li><li>Moonshot AI</li><li>ByteDance</li><li>Hugging Face</li><li>Baseten</li></ul><p>Each provider may have different configuration methods.</p><p>For example, if you are using <strong>OpenAI Codex</strong>, you can choose <strong>OpenAI Codex</strong> from the provider list and sign in directly with your ChatGPT account.</p><p>After signing in, Breezell can route Codex requests through the official Codex backend. This option does not require a separate API key.</p><p><img${ssrRenderAttr("src", _imports_2)} alt="OpenAI Codex provider page with ChatGPT account sign-in and Codex usage details"></p><h2 id="use-openai-compatible-for-a-custom-api" tabindex="-1">Use OpenAI-Compatible for a custom API <a class="header-anchor" href="#use-openai-compatible-for-a-custom-api" aria-label="Permalink to &quot;Use OpenAI-Compatible for a custom API&quot;">​</a></h2><p>If you want to use a third-party API or a self-hosted API gateway, choose <strong>OpenAI-Compatible</strong>.</p><p>This is the default custom API provider.</p><p>Unlike fixed providers, <strong>OpenAI-Compatible</strong> allows you to configure your own connection details manually.</p><p>You can set information such as:</p><ul><li>Provider nickname</li><li>Base URL</li><li>API key</li><li>Protocol</li><li>Endpoint preset</li><li>Model name</li><li>Custom headers</li></ul><p>This makes it suitable for custom gateways, third-party API platforms, proxy services, or OpenAI-compatible model providers.</p><p><img${ssrRenderAttr("src", _imports_3)} alt="OpenAI-Compatible in the Providers section"></p><h2 id="open-the-custom-api-configuration-panel" tabindex="-1">Open the custom API configuration panel <a class="header-anchor" href="#open-the-custom-api-configuration-panel" aria-label="Permalink to &quot;Open the custom API configuration panel&quot;">​</a></h2><p>In the <strong>OpenAI-Compatible</strong> section, find the default slot.</p><p>It may appear as <strong>OpenAI-Compatible 1</strong>.</p><p>You can click the slot area or click <strong>Manage slots</strong> on the right side.</p><p><img${ssrRenderAttr("src", _imports_4)} alt="OpenAI-Compatible slot entry with the default slot and Manage slots action"></p><p>This will open the custom API configuration panel.</p><p>Inside this panel, you can configure the connection, authentication, protocol, endpoint, and available models for your custom API provider.</p><p><img${ssrRenderAttr("src", _imports_5)} alt="OpenAI-Compatible provider configuration panel with connection and model settings"></p><h2 id="manage-provider-slots" tabindex="-1">Manage provider slots <a class="header-anchor" href="#manage-provider-slots" aria-label="Permalink to &quot;Manage provider slots&quot;">​</a></h2><p>At the top-left of the <strong>OpenAI-Compatible Providers</strong> panel, you will see an <strong>Available</strong> selector.</p><p>This selector shows the available custom API slots.</p><p>Breezell supports up to <strong>50 OpenAI-Compatible provider slots</strong>. Each slot can be configured as an independent custom API channel.</p><p>For example, you can use different slots for different providers, gateways, API keys, or model groups.</p><p>Click the <strong>Available</strong> selector to open the slot list.</p><p><img${ssrRenderAttr("src", _imports_6)} alt="Available selector showing custom API slots from 1 to 50"></p><p>You will see slots numbered from <strong>1</strong> to <strong>50</strong>.</p><p>If you are currently configuring <strong>Slot 1</strong>, you can select <strong>Slot 2</strong> to configure another custom API channel.</p><p>After selecting another slot, the configuration panel will switch to that slot. You can then enter a different provider name, Base URL, API key, protocol, endpoint, and model list for the new channel.</p><h2 id="configure-the-connection" tabindex="-1">Configure the connection <a class="header-anchor" href="#configure-the-connection" aria-label="Permalink to &quot;Configure the connection&quot;">​</a></h2><p>Return to the main configuration panel.</p><p>In the <strong>Connection</strong> section, you can configure the basic connection information for this custom API provider.</p><p><img${ssrRenderAttr("src", _imports_7)} alt="Connection section for a custom API provider with nickname, Base URL, API key, protocol, and models"></p><p>You can set:</p><ul><li>Nickname</li><li>Base URL</li><li>API Key</li><li>Protocol</li><li>Endpoint preset</li><li>Custom headers</li><li>Model names</li></ul><p>The <strong>Nickname</strong> is used to identify this custom provider. You can enter a name such as the provider name, gateway name, or any label that helps you recognize this channel.</p><p>The <strong>Base URL</strong> is the API gateway address.</p><p>The Base URL must start with either:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>http://</span></span></code></pre></div><p>or:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>https://</span></span></code></pre></div><p>For example:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>https://api.example.com/v1</span></span></code></pre></div><p>or:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>http://127.0.0.1</span></span></code></pre></div><p>If the Base URL does not start with <code>http://</code> or <code>https://</code>, the request may fail.</p><h2 id="check-the-final-request-url" tabindex="-1">Check the final request URL <a class="header-anchor" href="#check-the-final-request-url" aria-label="Permalink to &quot;Check the final request URL&quot;">​</a></h2><p>After entering the Base URL and selecting the endpoint preset, check the generated <strong>Request URL</strong> shown below the protocol section.</p><p>This area shows the final request address that Breezell will use.</p><p>For example, if your Base URL is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>http://127.0.0.1</span></span></code></pre></div><p>and the endpoint preset is <strong>OpenAI Generic</strong>, Breezell may generate:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>http://127.0.0.1/v1/chat/completions</span></span></code></pre></div><p>You should carefully confirm that the final Request URL is correct.</p><p>This is important because different providers may define their gateway URLs differently.</p><p>Some providers require the Base URL to include <code>/v1</code>, while others only require the root domain.</p><p>For example, one provider may require:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>https://api.example.com/v1</span></span></code></pre></div><p>while another provider may require:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>https://api.example.com</span></span></code></pre></div><p>If the provider uses a non-standard gateway format, the automatic endpoint completion may not work correctly.</p><p>This can cause the final request path, such as <code>/v1/chat/completions</code>, to be duplicated, missing, or incorrectly combined.</p><p>Before saving or using the provider, always check the generated <strong>Request URL</strong> and make sure it matches the API documentation from your provider.</p><p><img${ssrRenderAttr("src", _imports_8)} alt="Request URL preview below the protocol section for an OpenAI-Compatible provider"></p><h2 id="select-the-request-protocol" tabindex="-1">Select the request protocol <a class="header-anchor" href="#select-the-request-protocol" aria-label="Permalink to &quot;Select the request protocol&quot;">​</a></h2><p>In the <strong>Protocol</strong> section, choose the request protocol that matches your provider.</p><p>Common options include:</p><ul><li>OpenAI</li><li>Responses</li><li>Anthropic</li></ul><p>For most OpenAI-compatible third-party APIs, select <strong>OpenAI</strong>.</p><p>If your provider uses the OpenAI Responses API, select <strong>Responses</strong>.</p><p>If your provider or gateway expects Anthropic-compatible requests, select <strong>Anthropic</strong>.</p><p>Choose the protocol according to the API format required by your provider. If the protocol does not match the provider, requests may fail even if the Base URL and API key are correct.</p><h2 id="choose-the-endpoint-preset" tabindex="-1">Choose the endpoint preset <a class="header-anchor" href="#choose-the-endpoint-preset" aria-label="Permalink to &quot;Choose the endpoint preset&quot;">​</a></h2><p>Protocol selection is very important because it determines whether your model can be requested correctly.</p><p>Even when a provider says it is compatible with the OpenAI API, the actual request path may still be different from the official OpenAI API specification.</p><p>Some providers modify the standard endpoint structure. Breezell has adapted many of these provider-specific variants.</p><p>Click the endpoint preset dropdown to view the available presets.</p><p><img${ssrRenderAttr("src", _imports_9)} alt="Endpoint preset dropdown showing provider-specific request path options"></p><p>You may see options such as:</p><ul><li>OpenAI Generic</li><li>Zhipu GLM</li><li>Volcengine Ark</li><li>Volcengine Coding</li><li>DashScope Qwen</li><li>DeepSeek</li><li>Images</li></ul><p>Each preset represents a different request path.</p><p>For example, <strong>OpenAI Generic</strong> may use:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>/v1/chat/completions</span></span></code></pre></div><p>Other providers may use different endpoint paths, such as:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>/api/paas/v4/chat/completions</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>/api/v3/chat/completions</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>/compatible-mode/v1/chat/completions</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>/chat/completions</span></span></code></pre></div><p>Select the endpoint preset that matches your provider.</p><p>If you are not sure which preset to use, check the API documentation from your provider and compare the final <strong>Request URL</strong> shown in Breezell.</p><p>If your provider uses a format that Breezell has not adapted yet, please contact us so we can add support for that provider.</p><h2 id="configure-custom-headers" tabindex="-1">Configure custom headers <a class="header-anchor" href="#configure-custom-headers" aria-label="Permalink to &quot;Configure custom headers&quot;">​</a></h2><p>The <strong>Custom Headers</strong> section is optional.</p><p>Use this section only when your provider, gateway, or proxy service requires additional HTTP headers.</p><p>Custom headers are appended to every request sent through the current custom API slot.</p><p>The value must be valid JSON.</p><p><img${ssrRenderAttr("src", _imports_10)} alt="Custom Headers field with a JSON example and preset shortcuts"></p><p>For example:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;X-Custom-Header&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;value&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>You can use custom headers for provider-specific requirements, such as:</p><ul><li>Special routing headers</li><li>Organization or project headers</li><li>Proxy authentication headers</li><li>Beta feature headers</li><li>Long-context feature headers</li></ul><p>If your provider does not require custom headers, you can leave this section empty or keep it as an empty JSON object.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{}</span></span></code></pre></div><p>Make sure the JSON format is valid.</p><p>Use double quotes for both keys and values. Do not use comments, trailing commas, or single quotes.</p><p>Correct example:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;X-Provider-Mode&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;compatible&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Wrong example:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-dark": "#FDAEB7", "--shiki-light-font-style": "italic", "--shiki-dark-font-style": "italic" })}">  &#39;X-Provider-Mode&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-dark": "#FDAEB7", "--shiki-light-font-style": "italic", "--shiki-dark-font-style": "italic" })}">&#39;compatible&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Below the custom headers field, Breezell may provide preset shortcuts.</p><p>For example, the <strong>Claude 1M</strong> preset can automatically fill in the header required to enable Claude 1M context support.</p><p>If your provider documentation requires a specific header, copy that header into this section and confirm that it is valid JSON before sending requests.</p><h2 id="fetch-and-add-available-models" tabindex="-1">Fetch and add available models <a class="header-anchor" href="#fetch-and-add-available-models" aria-label="Permalink to &quot;Fetch and add available models&quot;">​</a></h2><p>After you finish configuring the connection information, click <strong>Fetch Available Models</strong> on the right side of the panel.</p><p>Breezell will try to request the provider and load the available model list.</p><p>If the request succeeds, the available models will appear in the model selection list.</p><p><img${ssrRenderAttr("src", _imports_11)} alt="Fetch Available Models button with available models loaded in the selection list"></p><p>After you confirm that the provider settings are correct, you can click this button to fetch and select your own models.</p><p>You can then select the models you want to use and click <strong>Add 1 Model</strong>.</p><p>For example, you can select a model such as:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>gpt-5.5</span></span></code></pre></div><p>Then click <strong>Add 1 Model</strong> to add it to the current custom API provider.</p><p>If the left area is still empty at this step, restart Breezell and try again.</p><p><img${ssrRenderAttr("src", _imports_12)} alt="Left area still empty after fetching available models"></p><p>You can also use <strong>Check All</strong> to test whether the gateway connection is healthy after configuring the provider and trying to load models.</p><p><img${ssrRenderAttr("src", _imports_13)} alt="Check All button for testing provider and gateway health"></p><p>If the check succeeds, the connection card can show a status such as <strong>Healthy</strong>. You can also use <strong>Recheck</strong> to test the gateway again after changing the configuration.</p><p><img${ssrRenderAttr("src", _imports_14)} alt="Connection card showing a healthy gateway status after a successful check"></p><h2 id="add-a-model-manually" tabindex="-1">Add a model manually <a class="header-anchor" href="#add-a-model-manually" aria-label="Permalink to &quot;Add a model manually&quot;">​</a></h2><p>You can also add a model manually.</p><p>In the model input field, enter the model name provided by your API provider.</p><p>Then click <strong>Add</strong>.</p><p>This is useful when the provider does not expose a model list endpoint, or when the model you want to use is not returned by <strong>Fetch Available Models</strong>.</p><p>Make sure the model name is exactly the same as the model ID required by your provider.</p><p><img${ssrRenderAttr("src", _imports_15)} alt="Manual model input field with the Add button"></p><h2 id="enable-disable-or-delete-a-model" tabindex="-1">Enable, disable, or delete a model <a class="header-anchor" href="#enable-disable-or-delete-a-model" aria-label="Permalink to &quot;Enable, disable, or delete a model&quot;">​</a></h2><p>After adding models, they will appear in the <strong>Models</strong> list.</p><p>Each model item can be managed individually.</p><p>You can use the switch on the right side of the model item to enable or disable the model.</p><p>When a model is enabled, Breezell can use it for requests.</p><p>When a model is disabled, Breezell will keep the configuration but will not use that model.</p><p>Move your mouse over a model item to show more actions.</p><p>You can delete a model by clicking the delete button.</p><p>Use this when you no longer want the model to appear in the current provider.</p><p><img${ssrRenderAttr("src", _imports_16)} alt="Model list with enable, disable, settings, and delete controls"></p><h2 id="open-advanced-model-settings" tabindex="-1">Open advanced model settings <a class="header-anchor" href="#open-advanced-model-settings" aria-label="Permalink to &quot;Open advanced model settings&quot;">​</a></h2><p>If you want to configure more detailed behavior for a model, click the settings button on the model item.</p><p>This opens the <strong>Advanced Settings</strong> panel.</p><p>Advanced settings are optional. Most users do not need to change them unless they understand the model limits and request format required by their provider.</p><h2 id="advanced-settings" tabindex="-1">Advanced Settings <a class="header-anchor" href="#advanced-settings" aria-label="Permalink to &quot;Advanced Settings&quot;">​</a></h2><p>In the advanced settings panel, you can customize model-level behavior.</p><p><img${ssrRenderAttr("src", _imports_17)} alt="Advanced Settings panel for a custom API model"></p><h3 id="enable-custom-settings" tabindex="-1">Enable custom settings <a class="header-anchor" href="#enable-custom-settings" aria-label="Permalink to &quot;Enable custom settings&quot;">​</a></h3><p>The <strong>Enable custom settings</strong> switch controls whether this model uses its own advanced configuration.</p><p>If this switch is disabled, Breezell will use the default recognized settings for the model.</p><p>If this switch is enabled, the values in the advanced settings panel will override the default behavior for this model.</p><h3 id="context-window" tabindex="-1">Context Window <a class="header-anchor" href="#context-window" aria-label="Permalink to &quot;Context Window&quot;">​</a></h3><p><strong>Context Window</strong> defines the maximum context length supported by the model.</p><p>This usually means the maximum number of tokens the model can process in one request, including conversation history, system messages, user messages, tool results, and other context.</p><p>For example, a value like:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>400000</span></span></code></pre></div><p>means the model is configured with a 400k context window.</p><p>The field supports <code>k</code> suffix auto-conversion.</p><p>For example:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>128k</span></span></code></pre></div><p>will be converted to:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>128000</span></span></code></pre></div><p>Use the value provided by your model provider. Setting this value too high may cause request failures if the model does not actually support it.</p><h3 id="output-token-space" tabindex="-1">Output Token Space <a class="header-anchor" href="#output-token-space" aria-label="Permalink to &quot;Output Token Space&quot;">​</a></h3><p><strong>Output Token Space</strong> defines the maximum output token space reserved for model responses.</p><p>For example, a value like:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>128000</span></span></code></pre></div><p>means the model can reserve up to 128k tokens for output.</p><p>This also supports <code>k</code> suffix auto-conversion.</p><p>For example:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>8k</span></span></code></pre></div><p>will be converted to:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>8000</span></span></code></pre></div><p>Use a value that matches the real output limit of your provider.</p><h3 id="system-message" tabindex="-1">System Message <a class="header-anchor" href="#system-message" aria-label="Permalink to &quot;System Message&quot;">​</a></h3><p><strong>System Message</strong> controls how Breezell sends system-level instructions to the model.</p><p>Some models expect the system message to use a standard <code>system</code> role.</p><p>Other models may expect a different role format, such as a <code>developer</code> role.</p><p>If your provider requires a specific system message format, select the matching option here.</p><p>If you are not sure, keep the default setting.</p><h3 id="tool-format" tabindex="-1">Tool Format <a class="header-anchor" href="#tool-format" aria-label="Permalink to &quot;Tool Format&quot;">​</a></h3><p><strong>Tool Format</strong> controls how tools are described and sent to the model.</p><p>For OpenAI-compatible models, use <strong>OpenAI Style</strong>.</p><p>If the provider requires a different tool calling format, select the format that matches the provider documentation.</p><p>Incorrect tool format settings may cause tool calling to fail.</p><h3 id="supports-thinking" tabindex="-1">Supports Thinking <a class="header-anchor" href="#supports-thinking" aria-label="Permalink to &quot;Supports Thinking&quot;">​</a></h3><p><strong>Supports Thinking</strong> indicates whether the model supports reasoning or thinking mode.</p><p>Enable this only when the model provider supports this feature.</p><h3 id="can-disable-thinking" tabindex="-1">Can Disable Thinking <a class="header-anchor" href="#can-disable-thinking" aria-label="Permalink to &quot;Can Disable Thinking&quot;">​</a></h3><p><strong>Can Disable Thinking</strong> indicates whether Breezell is allowed to disable thinking mode for this model.</p><p>This is useful for models that support both reasoning and non-reasoning modes.</p><h3 id="can-stream-reasoning" tabindex="-1">Can Stream Reasoning <a class="header-anchor" href="#can-stream-reasoning" aria-label="Permalink to &quot;Can Stream Reasoning&quot;">​</a></h3><p><strong>Can Stream Reasoning</strong> indicates whether the model can stream reasoning content during generation.</p><p>Only enable this if the provider supports streamed reasoning output.</p><p>If the provider does not support it, enabling this option may cause request errors or incomplete responses.</p><h2 id="save-advanced-settings" tabindex="-1">Save advanced settings <a class="header-anchor" href="#save-advanced-settings" aria-label="Permalink to &quot;Save advanced settings&quot;">​</a></h2><p>After changing advanced settings, click <strong>Save</strong>.</p><p>If you do not want to apply the changes, click <strong>Cancel</strong> or close the panel.</p><p>Only change advanced settings when you know the correct values for your model. Incorrect context size, output token limit, message format, tool format, or thinking settings may cause the model to behave incorrectly or fail to respond.</p><h2 id="select-your-custom-model" tabindex="-1">Select your custom model <a class="header-anchor" href="#select-your-custom-model" aria-label="Permalink to &quot;Select your custom model&quot;">​</a></h2><p>After adding and enabling your custom model, return to the chat interface.</p><p>Click the model selector near the input box.</p><p>In the model list, you will see the built-in Breezell models, Codex models, and your custom OpenAI-Compatible models.</p><p>Your custom provider will appear under its own provider group. For example, if your provider nickname is <strong>TEST</strong>, the models you added under that provider will appear in the <strong>TEST</strong> group.</p><p>Select the model you want to use.</p><p>If you added multiple models, choose the one that matches your current task.</p><p><img${ssrRenderAttr("src", _imports_18)} alt="Model selector showing built-in models and the TEST custom provider group"></p><h2 id="adjust-context-and-reasoning-options" tabindex="-1">Adjust context and reasoning options <a class="header-anchor" href="#adjust-context-and-reasoning-options" aria-label="Permalink to &quot;Adjust context and reasoning options&quot;">​</a></h2><p>After selecting a model, you can open the model options panel.</p><p>In this panel, you may see options such as:</p><ul><li>Context size</li><li>Reasoning mode</li><li>Reasoning strength</li><li>Fast mode</li></ul><p>The <strong>Context</strong> option controls how much conversation history or input context the model can use.</p><p>The <strong>Reasoning</strong> option controls whether reasoning mode is enabled.</p><p>If reasoning is enabled, you can choose the reasoning strength, such as:</p><ul><li>Low</li><li>Medium</li><li>High</li><li>Extra High</li></ul><p>Higher reasoning strength may be useful for complex coding, planning, debugging, or analysis tasks.</p><p>Lower reasoning strength may be better for simple questions or faster responses.</p><p>If the model supports <strong>Fast</strong> mode, you can enable it to prioritize faster response speed.</p><p>The available options may be different depending on the model and provider you selected.</p><p><img${ssrRenderAttr("src", _imports_19)} alt="Model options panel with context and reasoning settings"></p><h2 id="start-a-conversation" tabindex="-1">Start a conversation <a class="header-anchor" href="#start-a-conversation" aria-label="Permalink to &quot;Start a conversation&quot;">​</a></h2><p>After selecting the model and adjusting the options, type your message in the input box.</p><p>Then send the message to start a conversation.</p><p>For example, you can enter:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>Hello</span></span></code></pre></div><p>If the custom API is configured correctly, Breezell will send the request through your custom provider and return the model response in the chat.</p><p>You have now completed the custom API setup.</p><p><img${ssrRenderAttr("src", _imports_20)} alt="Chat view after sending a message through the custom model"></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("custom-api.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const customApi = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  customApi as default
};
